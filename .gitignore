# Ignore paths that contain user-generated content.
docroot/files
docroot/sites/*/files
docroot/sites/*/private

# Ignoring local files generated by Acquia Dev Desktop
docroot/sites/*.local

# Ignoring Netbeans projects
docroot/nbproject

# Also we don't want to track out local settings
settings.local.php

# We don't want to track 'active' configuration, only the staging
config/active
